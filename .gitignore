node_modules/
dist/
.DS_Store
*.local
.env*
!.env.example
coverage/
*.tsbuildinfo
playwright-report/
test-results/
CLAUDE.local.md

# Local reference implementation; not part of the distributable template.
/example/

# Instructor-owned slide decks stay local. Source locators remain in the project,
# but the original PowerPoint files are never published with the site.
/materials/slides/*.pptx
/materials/slides/*.ppt
